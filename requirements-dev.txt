# Development / test dependencies.
#
# Install alongside the runtime requirements::
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Or install via the ``dev`` extra declared in ``pyproject.toml``::
#
#     pip install '.[dev]'

-r requirements.txt

pytest>=8.0.0,<10
pytest-asyncio>=0.23.0,<2
pytest-cov>=5.0.0,<7
mypy>=1.11.0,<2
pip-audit>=2.7.0,<3

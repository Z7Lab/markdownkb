# mdkb environment configuration
# Copy to .env and adjust as needed: cp .env.example .env

# ── Server ────────────────────────────────────────
API_PORT=9713
FRONTEND_PORT=9714

# ── Docker ────────────────────────────────────────
MDKB_CONTAINER=mdkb
MDKB_PORT=9713
# Bind address (Docker only): localhost by default.
# Uncomment to allow access from other machines on your network.
# If you do, also set MDKB_API_KEY below to protect the API.
# MDKB_HOST=0.0.0.0

# ── LLM Providers ────────────────────────────────
# Ollama remote server — set this if Ollama runs on a different machine.
# See docs/ollama-remote-setup.md for secure setup instructions.
# OLLAMA_API_BASE=http://192.168.x.x:11434

# API keys (optional — only needed if using cloud providers)
# Preferred: use Docker secrets instead (see secrets/README.md)
# These env vars are a fallback when secrets/ files are not set.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# VENICE_API_KEY=

# ── Authentication ──────────────────────────────
# Set a key to protect all /api/ endpoints (optional).
# When empty or unset, authentication is disabled.
# Pick any string, or generate one:  openssl rand -hex 16
# MDKB_API_KEY=

# Runtime dependencies for the MarkdownKB server and MCP process.
#
# This file is kept as a convenience for ``pip install -r requirements.txt``
# in Docker builds; the canonical source of truth is ``[project.dependencies]``
# in ``pyproject.toml``. Dev/test dependencies live in
# ``[project.optional-dependencies].dev`` and are installed with
# ``pip install '.[dev]'`` or via ``requirements-dev.txt``.

# Core
chromadb>=0.4.22,<2
onnxruntime>=1.17.0,<2
tokenizers>=0.15.0,<1
anthropic>=0.40.0,<1
openai>=1.50.0,<2
pyyaml>=6.0.1,<7
python-frontmatter>=1.1.0,<2

# File watching
watchdog>=4.0.0,<7

# Hybrid search
rank-bm25>=0.2.2,<1

# API
fastapi>=0.110.0,<1
uvicorn>=0.27.0,<1
slowapi>=0.1.9,<1

# MCP server
mcp>=1.0.0,<2

# Utilities
numpy>=1.24.0,<3
httpx>=0.27.0,<1
platformdirs>=4.0.0,<5

# Document conversion
markitdown>=0.1.0,<1

# Knowledge graph plugin
scikit-learn>=1.4.0,<2

# MCP Terminal Tool Configuration
# Execute safe terminal commands via MCP protocol

allowed_commands:  # Whitelist of safe commands
  - ls
  - pwd
  - echo
  - cat
  - grep
  - find

timeout: 30  # Command timeout in seconds

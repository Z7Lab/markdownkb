"""Core domain modules — scope resolution, tag registry.

These modules hold load-bearing business logic used across routers and
services. They are intentionally separate from plugins so that core can
function without any plugin enabled.
"""
